import bcrypt from "bcryptjs";import {PrismaClient} from "../app/generated/prisma/client";import {PrismaPg} from "@prisma/adapter-pg";const adapter=new PrismaPg({connectionString:process.env.DATABASE_URL!});const db=new PrismaClient({adapter});async function main(){const email=process.env.ADMIN_EMAIL||"admin@example.com";const pass=process.env.ADMIN_PASSWORD||"ChangeMe123!";await db.adminUser.upsert({where:{email},update:{},create:{email,passwordHash:await bcrypt.hash(pass,12),name:"Elderz Administrator"}});if(!(await db.siteSettings.findFirst()))await db.siteSettings.create({data:{logoUrl:"/elderz-logo.png"}});if(!(await db.service.count()))await db.service.createMany({data:[{title:"Property Sales",description:"Professional guidance for buying property.",icon:"home",sortOrder:1},{title:"Property Rentals",description:"Find suitable rental properties.",icon:"key",sortOrder:2},{title:"Property Management",description:"Reliable management for property owners.",icon:"building",sortOrder:3}]});if(!(await db.testimonial.count()))await db.testimonial.create({data:{name:"Client Placeholder",role:"Property Client",quote:"Elderz Real Estate made the process clear and professional.",photoUrl:""}});console.log("Elderz seed complete")};main().finally(()=>db.$disconnect());