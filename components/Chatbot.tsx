"use client";
import {useState} from 'react';
export default function Chatbot(){const [open,setOpen]=useState(false),[input,setInput]=useState(''),[messages,setMessages]=useState<any[]>([{role:'assistant',content:'Hello! I’m the Elderz Real Estate assistant. How can I help you find a property or learn about our services?'}]),[busy,setBusy]=useState(false);async function send(){const text=input.trim();if(!text||busy)return;const next=[...messages,{role:'user',content:text}];setMessages(next);setInput('');setBusy(true);const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({messages:next})});const d=await r.json().catch(()=>({}));setMessages(v=>[...v,{role:'assistant',content:d.reply||d.error||'Sorry, I’m unavailable right now.'}]);setBusy(false)}return <div className={`chat ${open?'open':''}`}><button className="chatfab" onClick={()=>setOpen(!open)} aria-label="Open AI assistant">{open?'×':'AI'}</button>{open&&<div className="chatbox"><div className="chathead"><div><strong>Elderz AI</strong><span>Real estate assistant</span></div><button onClick={()=>setOpen(false)}>×</button></div><div className="chatmsgs">{messages.map((m,i)=><div key={i} className={`bubble ${m.role}`}>{m.content}</div>)}{busy&&<div className="bubble assistant">Thinking…</div>}</div><div className="chatinput"><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Ask about properties…"/><button onClick={send}>Send</button></div></div>}</div>}
