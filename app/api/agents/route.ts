import {NextResponse} from "next/server";import {db} from "@/lib/prisma";import {getSession} from "@/lib/auth";
export async function POST(req:Request){try{if(!(await getSession()))return NextResponse.json({error:"Unauthorized"},{status:401});const b=await req.json();if(!b.name||!b.role)return NextResponse.json({error:"Name and role are required"},{status:400});const a=await db.agent.create({data:{name:String(b.name),role:String(b.role),bio:String(b.bio||""),photoUrl:String(b.photoUrl||""),phone:String(b.phone||""),email:String(b.email||""),whatsapp:String(b.whatsapp||""),status:"VERIFIED",emailVerified:true,phoneVerified:true,identityVerified:true,licenseVerified:true,backgroundCheckVerified:true,acceptedAt:new Date(),reviewedAt:new Date()}});return NextResponse.json(a,{status:201})}catch(e:any){return NextResponse.json({error:e?.message||"Server error"},{status:500})}}