import {NextResponse} from 'next/server';import {db} from '@/lib/prisma';export async function POST(req:Request){try{const b=await req.json();if(!b.name||!b.email||!b.message)return NextResponse.json({error:'Missing fields'},{status:400});await db.inquiry.create({data:{name:b.name,email:b.email,phone:b.phone||'',message:b.message,propertyId:null}});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Server error'},{status:500})}}
