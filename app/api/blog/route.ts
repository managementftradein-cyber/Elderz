import {NextResponse} from "next/server";import {db} from "@/lib/prisma";import {requireAdmin} from "@/lib/auth";import {slugify} from "@/lib/slug";export async function POST(req:Request){try{await requireAdmin();const b=await req.json();const published=Boolean(b.published);return NextResponse.json(await db.blogPost.create({data:{title:b.title,slug:slugify(b.title)+"-"+Date.now(),excerpt:b.excerpt||"",content:b.content||"",imageUrl:b.imageUrl||"",published,publishedAt:published?new Date():null}}),{status:201})}catch(e:any){return NextResponse.json({error:e?.message||"Error"},{status:e?.message==="UNAUTHORIZED"?401:500})}}