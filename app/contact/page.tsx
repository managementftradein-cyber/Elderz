'use client';
import {useState} from 'react';
export default function Contact(){const [sent,setSent]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/inquiries',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(r.ok)setSent(true)}return <main className="section"><div className="container" style={{maxWidth:760}}><div className="eyebrow">Contact</div><h1>Let’s talk property.</h1>{sent?<div className="panel">Thank you. Your enquiry has been received.</div>:<form className="panel formgrid" onSubmit={submit}><label className="field"><span>Name</span><input name="name" required/></label><label className="field"><span>Email</span><input name="email" type="email" required/></label><label className="field"><span>Phone</span><input name="phone"/></label><label className="field"><span>Property</span><input name="property"/></label><label className="field full"><span>Message</span><textarea name="message" required/></label><button className="btn gold" type="submit">Send enquiry</button></form>}</div></main>}
