import {getSession} from '@/lib/auth';import {redirect} from 'next/navigation';import {db} from '@/lib/prisma';import SettingsForm from './settings-form';export const dynamic='force-dynamic';export default async function Settings(){if(!(await getSession()))redirect("/admin/login");const s=await db.siteSettings.findFirst();return <><h1>Site settings</h1><p className="muted">Everything here feeds the public website.</p><SettingsForm settings={s}/></>}
