import {db} from '@/lib/prisma';import {requireAdmin} from '@/lib/auth';import SettingsForm from './settings-form';export const dynamic='force-dynamic';export default async function Settings(){await requireAdmin();const s=await db.siteSettings.findFirst();return <><h1>Site settings</h1><p className="muted">Everything here feeds the public website.</p><SettingsForm settings={s}/></>}
