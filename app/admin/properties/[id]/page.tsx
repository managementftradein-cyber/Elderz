import {getSession} from '@/lib/auth';import {redirect} from 'next/navigation';import {db} from '@/lib/prisma';import EditPropertyForm from './form';import {notFound} from 'next/navigation';export const dynamic='force-dynamic';export default async function EditProperty({params}:{params:Promise<{id:string}>}){if(!(await getSession()))redirect("/admin/login");const {id}=await params;const p=await db.property.findUnique({where:{id}});if(!p)return notFound();return <><h1>Edit property</h1><EditPropertyForm p={p}/></>}
