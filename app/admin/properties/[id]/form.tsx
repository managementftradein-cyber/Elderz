'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function EditPropertyForm({p}:{p:any}){const router=useRouter();const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const b:any=Object.fromEntries(new FormData(e.currentTarget));b.price=Number(b.price);b.bedrooms=Number(b.bedrooms);b.bathrooms=Number(b.bathrooms);b.area=Number(b.area);b.featured=b.featured==='on';const r=await fetch(`/api/properties/${p.id}`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});setMsg(r.ok?'Saved ✓':'Could not save.');if(r.ok)router.refresh()}async function remove(){if(!confirm('Delete this property?'))return;const r=await fetch(`/api/properties/${p.id}`,{method:'DELETE'});if(r.ok)router.push('/admin/properties')}return <form className="panel formgrid" onSubmit={submit}><label className="field"><span>Title</span><input name="title" defaultValue={p.title} required/></label><label className="field"><span>Type</span><input name="type" defaultValue={p.type} required/></label><label className="field"><span>Price</span><input name="price" type="number" defaultValue={p.price}/></label><label className="field"><span>Currency</span><input name="currency" defaultValue={p.currency}/></label><label className="field"><span>Status</span><select name="status" defaultValue={p.status}><option>FOR_SALE</option><option>FOR_RENT</option><option>SOLD</option><option>RENTED</option><option>DRAFT</option></select></label><label className="field"><span>Location</span><input name="location" defaultValue={p.location}/></label><label className="field"><span>Bedrooms</span><input name="bedrooms" type="number" defaultValue={p.bedrooms}/></label><label className="field"><span>Bathrooms</span><input name="bathrooms" type="number" defaultValue={p.bathrooms}/></label><label className="field"><span>Area</span><input name="area" type="number" defaultValue={p.area}/></label><label className="field full"><span>Main image URL</span><input name="imageUrl" defaultValue={p.imageUrl}/></label><label className="field full"><span>Description</span><textarea name="description" defaultValue={p.description}/></label><label className="field full"><span>Gallery URLs</span><textarea name="gallery" defaultValue={p.gallery.join('\n')}/></label><label className="field"><span><input name="featured" type="checkbox" defaultChecked={p.featured}/> Featured</span></label><div><button className="btn gold">Save changes</button><button type="button" className="btn secondary" style={{marginLeft:10}} onClick={remove}>Delete</button>{msg&&<span style={{marginLeft:10}}>{msg}</span>}</div></form>}
