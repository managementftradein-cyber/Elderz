'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function NewProperty(){const router=useRouter();const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const b:any=Object.fromEntries(new FormData(e.currentTarget));b.price=Number(b.price);b.bedrooms=Number(b.bedrooms);b.bathrooms=Number(b.bathrooms);b.area=Number(b.area);b.featured=b.featured==='on';const r=await fetch('/api/properties',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});if(r.ok)router.push('/admin/properties');else setError('Could not save property.')}return <><h1>Add property</h1><form className="panel formgrid" onSubmit={submit}><label className="field"><span>Title</span><input name="title" required/></label><label className="field"><span>Type</span><input name="type" placeholder="Apartment, House, Land" required/></label><label className="field"><span>Price</span><input name="price" type="number" required/></label><label className="field"><span>Currency</span><input name="currency" defaultValue="NGN"/></label><label className="field"><span>Status</span><select name="status"><option>FOR_SALE</option><option>FOR_RENT</option><option>SOLD</option><option>RENTED</option><option>DRAFT</option></select></label><label className="field"><span>Location</span><input name="location" required/></label><label className="field"><span>Bedrooms</span><input name="bedrooms" type="number" defaultValue="0"/></label><label className="field"><span>Bathrooms</span><input name="bathrooms" type="number" defaultValue="0"/></label><label className="field"><span>Area (sqm)</span><input name="area" type="number" defaultValue="0"/></label><label className="field"><span>Main image URL</span><input name="imageUrl" placeholder="https://..." required/></label><label className="field full"><span>Description</span><textarea name="description" required/></label><label className="field full"><span>Gallery URLs (one per line)</span><textarea name="gallery"/></label><label className="field"><span><input name="featured" type="checkbox"/> Featured property</span></label>{error&&<p style={{color:'crimson'}}>{error}</p>}<button className="btn gold" type="submit">Save property</button></form></>}
