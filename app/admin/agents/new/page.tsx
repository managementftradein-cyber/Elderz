"use client";
import {useState} from "react"; import {useRouter} from "next/navigation";
export default function NewAgent(){const r=useRouter();const [busy,setBusy]=useState(false);async function submit(e:any){e.preventDefault();setBusy(true);const b=Object.fromEntries(new FormData(e.currentTarget).entries());const res=await fetch('/api/agents',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});if(res.ok)r.push('/admin/agents');else setBusy(false)}return <><h1>Add agent</h1><form className="panel form-grid" onSubmit={submit}><div><label>Name</label><input className="input" name="name" required/></div><div><label>Role</label><input className="input" name="role" required/></div><div><label>Email</label><input className="input" name="email"/></div><div><label>Phone</label><input className="input" name="phone"/></div><div><label>WhatsApp</label><input className="input" name="whatsapp"/></div><div><label>Photo URL</label><input className="input" name="photoUrl"/></div><div style={{gridColumn:'1/-1'}}><label>Bio</label><textarea className="input" name="bio" rows={6}/></div><button className="btn dark" disabled={busy}>{busy?'Saving...':'Save agent'}</button></form></>}
