import {db} from "@/lib/prisma";export const dynamic="force-dynamic";export default async function Agents(){const a=await db.agent.findMany({orderBy:{createdAt:"desc"}});return <><h1>Agents</h1><div className="panel"><table className="table"><thead><tr><th>Name</th><th>Role</th><th>Email</th><th>Phone</th></tr></thead><tbody>{a.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.role}</td><td>{x.email}</td><td>{x.phone}</td></tr>)}</tbody></table><p>Add/edit agent forms can be wired to the same database model.</p></div></>;}