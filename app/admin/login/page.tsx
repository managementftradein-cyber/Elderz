'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Login(){const [error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});if(r.ok)router.push('/admin');else setError('Invalid login details.')}return <main className="login"><form className="loginbox" onSubmit={submit}><h1>Admin login</h1><p className="muted">Use your seeded administrator credentials.</p><label className="field"><span>Email</span><input name="email" type="email" required/></label><br/><label className="field"><span>Password</span><input name="password" type="password" required/></label>{error&&<p style={{color:'crimson'}}>{error}</p>}<button className="btn" style={{width:'100%',marginTop:18}}>Sign in</button></form></main>}
