import {notFound} from 'next/navigation';
import Link from 'next/link';
import {db} from '@/lib/prisma';
export const dynamic='force-dynamic';
export default async function AgentProfile({params}:{params:Promise<{id:string}>}){const {id}=await params;const a=await db.agent.findUnique({where:{id}});if(!a||a.status!=='VERIFIED')return notFound();return <main><section className="section cream"><div className="container agent-profile-head"><Link href="/agents" className="backlink">← All verified agents</Link><div className="agent-profile-grid"><div className="agent-profile-photo">{a.photoUrl?<img src={a.photoUrl} alt={a.name}/>:<span>{a.name.slice(0,1)}</span>}</div><div><div className="verified">✓ ELDERZ VERIFIED PROFESSIONAL</div><h1>{a.name}</h1><p className="profile-role">{a.role}</p><p className="lead">{a.bio}</p><div className="verification-grid"><span>✓ Identity verified</span>{a.licenseVerified&&<span>✓ License verified</span>}<span>✓ Contact verified</span>{a.backgroundCheckVerified&&<span>✓ Background checked</span>}</div><div className="agentcontact"><a className="btn-primary" href={`tel:${a.phone}`}>Call agent</a>{a.whatsapp&&<a className="btn" href={`https://wa.me/${a.whatsapp.replace(/\D/g,'')}`}>WhatsApp</a>}<a className="btn secondary" href={`mailto:${a.email}`}>Email</a></div></div></div></div></section><section className="section"><div className="container profile-service-grid"><div className="panel"><div className="eyebrow blue">Professional details</div><h2>Work with confidence.</h2><p>Every public Elderz agent has passed the verification workflow selected by the Elderz team. Verification status is reviewed before publication.</p><div className="profile-detail"><strong>License</strong><span>{a.licenseNumber||'Verified through Elderz review'}</span></div><div className="profile-detail"><strong>Specialization</strong><span>{a.role}</span></div></div><div className="panel agent-cta"><div className="eyebrow blue">Need help?</div><h2>Looking for a property professional?</h2><p>Tell us what you are buying, selling or renting and we can help you start a conversation.</p><Link className="btn-primary" href="/contact">Send an enquiry</Link></div></div></section></main>}
